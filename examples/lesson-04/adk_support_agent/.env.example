GOOGLE_GENAI_USE_ENTERPRISE=TRUE
GOOGLE_CLOUD_PROJECT=build-ai-systems-dev
GOOGLE_CLOUD_LOCATION=global

# Optional. The lesson uses this tested default when the variable is absent.
# SUPPORT_AGENT_MODEL=gemini-3.5-flash
