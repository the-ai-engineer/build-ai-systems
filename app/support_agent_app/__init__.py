"""HR policy support assistant.

Import from the owning component, for example
`support_agent_app.application.domain` or `support_agent_app.agent.agent`. This
module stays empty so importing the package never pulls in a model provider, a
database driver, or any other adapter.
"""
